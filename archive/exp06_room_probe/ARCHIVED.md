# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

엔티티 임베딩 직접 ward 클러스터(GRAPH arm 원형) 실험. `base_cluster` 함수의 디자인이 여기서 출발해 exp10.room_gen.base_cluster로 모듈화됐다. palace는 TOC arm 단일이라 base_cluster를 포함하지 않으며 이 폴더 코드도 복사하지 않았다. GRAPH arm 참고용으로만 남김.
