# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

Stage B n=3 다수결 안정성 측정. palace.room_gen.assign_rooms는 `n_runs` 인자와 같은 다수결 코드(_resolve_keep_membership)를 exp10에서 그대로 가져와 갖고 있다. 이 폴더의 측정 스크립트는 복사 안 함. 결정(앵커 recall은 n=1·n=3 동일, 비-앵커 ~7% churn에 n=3 권장)은 EXPERIMENTS.md exp12 섹션에 남아 있음.
