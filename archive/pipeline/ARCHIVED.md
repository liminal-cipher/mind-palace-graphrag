# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

GRAPH arm canonical 러너(K=10, n=3, embedding merge, repro_run3). palace TOC arm이 정본 자리를 가져가기 전 confirmed-pipeline 자리였음. palace에 코드 복사 없음(전혀 다른 파이프라인). 구 canonical, GRAPH arm 참고용으로 남김. 산출(`rooms.json`, `palace.json`, `cost_report.json`, `report.md`)은 GRAPH arm 비용·다수결 효과 측정치로서 그대로 유효.
