# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

LLM-only 방 설계(overlap200 step-3) 재현성 측정. palace 정본은 결정적 TOC 매핑 + LLM keep/demote 분리 구조라 LLM-only 배정을 채택하지 않는다. 이 폴더 코드는 복사 안 함. 결정(엔티티 단위 배정·노출은 결정적 단계로 끊어야 함)은 EXPERIMENTS.md exp14 섹션과 palace 설계 방향에 반영됨.
