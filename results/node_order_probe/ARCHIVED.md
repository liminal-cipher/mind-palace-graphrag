# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

palace 정본이 이 폴더에서 복사한 것:
- `node_metrics.py` 중 `build_text_unit_positions`, `_surface_variants`, `_count_in_chunk`, `_first_in_text`, `compute_entity_metrics` → `palace/node_metrics.py`

복사 안 한 것: 모듈 상수 `SNAPSHOT`/`TXT_PATH`(probe 전용 한국사 하드코딩), 보조 로더 `load_text`, `load_snapshot_frames`, `tie_cluster_sizes`, 그리고 probe 러너 `probe.py`와 산출(`node_position_weight.csv`, `rooms_ordered.md`, `REPORT.md`).
