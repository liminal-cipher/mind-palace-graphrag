# ARCHIVED

이 폴더는 동결. 새 작업은 `palace/` 기반.

목차 섹션을 방으로 쓸 수 있는지 진단. 청크가 섹션보다 커서 단일 섹션 매핑이 0이라는 결론(→ exp15에서 char-overlap argmax로 풀림). text_unit char-span 계산 아이디어가 node_order_probe.node_metrics로 이어졌고 palace.node_metrics가 그쪽에서 복사했다. 이 폴더 코드는 직접 복사 안 함.
