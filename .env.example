# Azure OpenAI 키. settings.yaml의 ${GRAPHRAG_API_KEY}와 exp5_llm.py가 모두 이 변수를 읽는다.
# .env.example을 .env로 복사한 뒤 값 채워서 사용. .env는 .gitignore로 커밋 막혀 있음.
GRAPHRAG_API_KEY=
GRAPHRAG_API_BASE=https://your-resource.openai.azure.com/
