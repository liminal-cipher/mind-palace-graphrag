.env
cache/
.venv/
__pycache__/
*.pyc

# graphrag 작업 산출물 (인덱싱마다 갱신)
output/
logs/

# palace 재실행 산출물 (palace/tests/golden 과 byte-identical, 재생성 가능)
palace/tests/runs/

# 입력 자료 (저작물)
input/
archive/exp09_rechunk/proj_pagesplit/input/
archive/exp09_rechunk/proj_semantic/input/
# 게시 이미지(deliverables/<domain>/images/)는 input/ 밖이라 그대로 추적됨

# 오케스트레이션 런타임 산출물 (잡 폴더 + SQLite, 재생성 가능)
var/

# 자료 전처리 결과
preprocessing/result/

# 원본 PDF(저작물). 규약 문서(README)만 추적.
preprocessing/source/*
!preprocessing/source/README.md

